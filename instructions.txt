- pnpm init
- pnpm i express dotenv @prisma/client @prisma/adapter-pg bcrypt jsonwebtoken zod
- pnpm i -D nodemon prisma
- create src folder, app.js, .env
- add variable to .env
- add script in package.json to start a dev server
- create express app object and listen to some port in app.js
- add essential middleware, parsing request body, not found, error handling
- create schema.prisma and write your model, change DATABASE_URL in .env
  table users
  - id PK, email unique, password, role ENUM(USER,ADMIN), created_at, updated_at, deleted_at(can be null)
  table todos
  - id PK, title, status, due_date, created_at, updated_at, user_id FK
- sync your postgres db with schema.prisma
- generate prisma client
- create validate middleware, authenticate middleware

DOMAIN: auth
- REGISTER
POST /auth/register
BODY
  email
  password
  confirm
RESPONSE
  201 message
  400 message
  409 message
  500 message

- LOGIN
POST /auth/login
BODY
  email
  password
RESPONSE
  200 access_token, user
  400 message
  401 message
  500 message

- ME
GET /auth/me
AUTHORIZATION Bearer access_token
RESPONSE
  200 user
  401 message
  500 message

ONLY USER
- CREATE TODO
POST /todos
AUTHORIZATION Bearer access_token
BODY
  title
  status?
  dueDate?
RESPONSE
  201 todo
  400 message
  401 message
  403 message
  500 message

- UPDATE TODO
PUT /todos/:id
AUTHORIZATION Bearer access_token
BODY
  title?
  status?
  dueDate?
RESPONSE
  200 todo
  400 message
  401 message
  403 message
  500 message

- GET ALL TODO
- DELETE TODO

ONLY ADMIN
- GET ALL USER
- DISABLE USER (SOFT DELETE)
POST /users/:id/disable
- ENABLE USER
POST /users/:id/enable